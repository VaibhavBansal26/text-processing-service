[pytest]
pythonpath = src
testpaths = tests
asyncio_mode = auto
markers =
    performance: performance and load style tests (run explicitly)
    integration: integration tests that may touch multiple components or persistence
    async_integration: async Option A end to end tests
addopts = -q
